import React from "react";
import { SiteWrapper } from "../components";

const Error404 = (): JSX.Element => {
  return (
    <SiteWrapper>The page you are trying to open was not found...</SiteWrapper>
  );
};

export default Error404;
